# macOS
.DS_Store
.AppleDouble
.LSOverride

# Development / planning files (not for distribution)
launch-plan.md
SUBMISSION-STEPS.md

# Local testing / environment files (never distribute)
temp-pro-enable.php
temp-*.php
wp-config.php

# Editor
*.swp
.idea/
.vscode/

# Composer (vendor is committed, but these shouldn't be)
/node_modules/

# Build artifacts / packaged zips (repo-only, never tracked)
/archives/
*.zip

# Reviewer correspondence / dev notes (not part of the plugin)
reviewer-reply.txt

# Cloud-mount temp files (Claude synced folder)
.fuse_hidden*
