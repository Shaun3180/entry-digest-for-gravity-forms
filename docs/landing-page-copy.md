# Entry Digest — Landing Page Copy Reference

Copy-paste reference for the promo page at
https://addasitebuilders.com/plugins/entry-digest-for-gravity-forms/

Everything below matches the voice of your existing cards. Font Awesome icons are
all **Free (Solid)** so they work in Elementor without a Pro Font Awesome license.
Keep every icon in the Solid style and one accent color so the free and Pro grids
read as a single family.

---

## 1. Two MISSING free cards

Add these to the existing **"Everything you need to stop drowning in form emails"** grid.

### One-Time Sends
**Icon:** `fa-calendar-day`

Need a digest for a specific date — an event recap or an end-of-campaign summary?
Schedule a one-time send for any future date and time. It goes out once, then clears
itself automatically, on its own or alongside your recurring schedule.

### Graceful When Quiet *(reworded — replaces the current version)*
**Icon:** `fa-bell-slash`

Slow periods happen, and you decide what they look like. Per digest, choose to receive
a tidy "no new entries" note so you know it ran — or stay completely silent until the
next submission arrives.

---

## 2. New PRO card section

Place this as its own grid (same card style as the free grid), directly above or below
the pricing table. Lead with a clear "Pro" heading so there's no ambiguity about what's
paid. **Move the existing "Conditional Filtering" card out of the free grid into this one.**

**Section heading suggestion:**
> ### Ready to scale? Pro adds the routing and flexibility teams need

### Unlimited Digests & Multi-Form
**Icon:** `fa-layer-group`

Run as many digests as you need, and roll several forms into one email. Aggregate your
contact, quote, and registration forms into a single morning summary instead of juggling
separate notifications.

### Per-Recipient & Role Routing
**Icon:** `fa-route`

Send the right digest to the right people. Route the admissions form to your admissions
lead and the support form to your help desk — by individual recipient or by WordPress role.

### Conditional Filtering *(move here from the free grid)*
**Icon:** `fa-filter`

Include only the entries that matter. Set rules like *Status is Complete* or
*Budget greater than 1000*, match all or any, and each digest filters itself
automatically on every run.

### CSV & Excel Attachments
**Icon:** `fa-file-csv`

Attach the full period's entries as a CSV or native Excel (.xlsx) file. Recipients get
the scannable summary in the email and the complete dataset ready to sort, filter, or import.

### Priority Support
**Icon:** `fa-headset`

Get fast, direct help from the developer. Pro licenses come with priority email support
so setup questions and edge cases don't sit in a queue.

> **Layout note:** Five cards grids unevenly (3 + 2). For a balanced 6-card grid, either
> split "Unlimited Digests" and "Multi-Form aggregation" into two cards, or drop Priority
> Support from the grid since it's already a bullet in your pricing table.

---

## 3. Two MISSING FAQs

Add both to your **"Common questions"** FAQ section.

**Q: Can I send a digest just once on a specific date?**

Yes. Each digest has an optional one-time send field: pick a future date and time and the
digest goes out once, then the date clears itself automatically. You can use it on its own
(choose "One-time only") or in addition to a daily/weekly schedule. A separate lookback
setting controls how far back the one-time send reaches for entries, and defaults to
everything since the form was created.

**Q: What happens when no new entries came in?**

By default the digest still sends a tidy "no new entries" note so recipients know it ran
and nothing was missed. If you'd rather stay silent during quiet periods, set that digest's
"When there are no new entries" option to "Don't send anything."

---

## 4. While you're in there — quick fixes flagged earlier

These aren't new copy, but worth fixing on the same pass:

- **Broken CTAs:** "Download Free On WordPress.org" and "View Pro Features" both link to
  `#`; the Free card's "Download Free" button links to `/services/`. Point them at your
  WordPress.org plugin page (once submitted) and the Pro section/pricing anchor.
- **Duplicate FAQ answer:** "Can I see an overview of the plugin's settings?" repeats the
  same answer as the "Where do I find settings?" question — fix or delete it.
- **Typo:** "When exactly are digests sent, and how are they triggered??" — double `??`.
- **Conditional Filtering** currently sits in the free grid but is a Pro feature — moving
  it (see section 2) resolves the inconsistency.

---

## 5. Icon set for the EXISTING free cards (for cohesion)

So the whole grid reads as one family:

| Card | Icon |
|------|------|
| Summary + Entry Table | `fa-table-list` |
| Daily or Weekly Scheduling | `fa-calendar-days` |
| Choose Your Fields | `fa-list-check` |
| Send Now Preview | `fa-paper-plane` |
| Zero Phone-Home | `fa-shield-halved` |
| One-Time Sends | `fa-calendar-day` |
| Graceful When Quiet | `fa-bell-slash` |
